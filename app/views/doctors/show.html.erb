<h1> <%= @doctor.name %> </h1>

<h2> <%= @doctor.department %> </h1>

<% @appointments.each do |appointment| %> 
    <%= appointment.appointment_time %><br> 
    <%= link_to appointment.patient.name, patient_path(appointment.patient) %><br>
    <br>
<% end %>