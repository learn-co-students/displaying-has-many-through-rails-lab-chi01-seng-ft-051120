<h1>Patient <%= @patient.name %>'s Page</h1>
<h3>Appointments:</h3>
  <% @patient.appointments.each do |a| %>
    <p><%= "#{a.appointment_datetime.strftime("%B %d, %Y at %H:%M")}"%></p>
    <em><%= link_to "#{a.doctor.name}", doctor_path(a.doctor) %></em>
  <% end %>
